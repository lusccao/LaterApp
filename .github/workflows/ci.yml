name: CI

on:
  pull_request:
    branches: [develop, main]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Build placeholder"